node_modules
.next
out
.packrest.config.json

# TypeScript incremental build state (rewritten by every `npm run typecheck`)
tsconfig.tsbuildinfo

# Playwright MCP session artifacts
.playwright-mcp/

# Playwright test artifacts (baselines under tests/**/-snapshots are committed)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Tauri
/src-tauri/target
/src-tauri/gen/schemas
